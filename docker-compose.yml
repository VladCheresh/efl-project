services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: vdzh
      POSTGRES_USER: vdzh
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: